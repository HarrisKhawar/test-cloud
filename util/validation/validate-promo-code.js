/**
// ! ---------------------------------- Validate Promo Code ---------------------------------- *
*/
// TODO: Generate one-time promo codes
const validatePromoCode = (code) => {
  return code === "PCIO2022";
};

module.exports = { validatePromoCode };
